#include <stdio.h>

int main(void)
{
    int height,breadth,volume,length;
    height = 8;
    breadth = 10;
    length = 12;
    volume = height*breadth* length;
    printf("Height = %d\nLength = %d\nBreadth = %d\nVolume = %d\n",height, length, breadth, volume);
    return 0;
}
